# Velytics API — pinned to the exact versions tested locally.
# Keep in sync with the venv: run `pip freeze` after any dependency change.

# Web framework
fastapi==0.136.3
uvicorn==0.49.0
python-multipart==0.0.31

# Data / analytics
pandas==2.2.3
numpy==2.4.6
openpyxl==3.1.5
scikit-learn==1.9.0

# Privacy / metadata engines
Pillow==12.2.0
pillow-heif==1.3.0
piexif==1.1.3
pypdf==6.13.1
imageio-ffmpeg==0.6.0
extract-msg==0.55.0

# Reports (PDF)
fpdf2==2.8.7
